from django.apps import AppConfig


class MessageTemplatesConfig(AppConfig):
    """AppConfig for the MessageTemplates app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'message_templates'
