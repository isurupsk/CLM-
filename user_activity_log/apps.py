from django.apps import AppConfig


class UserActivityLogConfig(AppConfig):
    """AppConfig for the UserActivityLog app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'user_activity_log'
