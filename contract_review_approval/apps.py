from django.apps import AppConfig


class ContractReviewApprovalConfig(AppConfig):
    """AppConfig for the Contract Review Approval app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'contract_review_approval'
